// creating an arrays
const arr1 = ["Volvo", "Royal Enfield", "Honda", "BMW"] // array of an string

const arr2 = [2, 4, 5, 6] // array of an numbers

const arr3 = ["String", 2, true, 2.12] // the array of mixed values

const arr4 = [] // an empty array

// accessing the element of an array
const firstString = arr1[0]
console.log("arr1: ", firstString)

const secString = arr1[1]
console.log("arr1: ", secString)

// length of an array

lenArr1 = arr1.length
console.log("length of an array of string: ", lenArr1)



const arr = [1, "2", true]
console.log(arr)

// iterating the array
for (let i = 0; i < arr.length; i++){
    console.log(arr[i])
}
