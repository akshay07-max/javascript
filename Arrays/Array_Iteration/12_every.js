// check if all the elements pass the test: (boolean)

const arr = [2, 4, 6, 8, 10, 12];

const evenArr = arr.every((num) => num % 2 === 0);  // return true if all the elements in the array has even numbers.

console.log(evenArr)

