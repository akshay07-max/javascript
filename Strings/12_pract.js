String methods:
1. toUpperCase
2. toLowerCase
3. split
4. join
5. searching (indexOf, includes, lastIndexof, startWith, endsWith)
6. trimming
7. template literals
8. replace
9. compare
10. convert to string

const str = "My name Anthony Gonsalves";

const toUpper = str.toUpperCase();
console.log(toUpper);

const spt = str.split(",");
console.log(spt);

const jn = str.join("-")